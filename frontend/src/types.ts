export interface DashboardData {
  calories: string;
  protein: string;
  carbs: string;
  fat: string;
}

export interface FoodEntry {
  id: number;
  name: string;
  servings: string;
  entry_date: string;
}